/* This file would normally contain a binary image. 
   Since we can't include binary files, we'll use an external image URL in the HTML instead.
   In a real implementation, you would replace this with an actual image file. */